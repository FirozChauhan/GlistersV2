/* Generated runtime config — js/config.js is generated from .env by
   `npm run gen-config` and is GITIGNORED (it carries your real GitHub
   token + wallhaven key, which must never be committed).

   The actual js/config.js is written by scripts/gen-config.mjs. To set up a
   fresh clone: copy .env.example → .env, fill in GIST_ID / GITHUB_TOKEN
   (optionally WALLHAVEN_API_KEY), then run `npm run build`.

   This example is a safe reference only — leave all values empty here. */
window.CONFIG = {
  "gistId": "",
  "githubToken": "",
  "wallhavenKey": "",
  "generatedAt": ""
};
